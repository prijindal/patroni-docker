on:
  push:
    branches:
      - "*"

name: Docker

permissions:
  contents: read
  packages: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# cancel in-progress runs
concurrency:
  group: "pages"
  cancel-in-progress: true


jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Login to docker
        uses: docker/login-action@v3
        with:
          registry: registry.prijindal.click
          username: prijindal
          password: ${{ secrets.REGISTRY_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: registry.prijindal.click/${{ gitea.repository }}:${{ gitea.ref_name }},registry.prijindal.click/${{ gitea.repository }}:${{ gitea.sha }}
          cache-from: type=registry,ref=registry.prijindal.click/${{ gitea.repository }}:buildcache
          cache-to: type=registry,ref=registry.prijindal.click/${{ gitea.repository }}:buildcache,mode=max
